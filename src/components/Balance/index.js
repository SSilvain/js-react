const Balance = ({ balance }) => {
    return (
        <>
            <h1>Баланс:</h1>
            <h2 style={{ padding: "0 0 0 20px" }}>{balance}</h2>
        </>
    );
};

export default Balance;
