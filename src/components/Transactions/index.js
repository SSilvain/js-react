const Transactions = ({ label, value }) => {
                return <li>
                    <div>{label}</div>
                    <div>{value}</div>
                </li>;
};

export default Transactions;
